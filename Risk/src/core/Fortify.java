package core;

public class Fortify {



/*
	public Fortify(Country takeArmies, Country putArmies, int amountMoved){

		for(int k=0; k<= Data.COUNTRY_NAMES.length-1;k++){
			if(takeArmies.getName().compareToIgnoreCase(Data.COUNTRY_NAMES[k])  == 1 || putArmies.getName().compareToIgnoreCase(Data.COUNTRY_NAMES[k]) == 1 ){
				System.out.println("Sorry this isnt a country");
			}
			else{
				for(int j =0; j<takeArmies.getAdjacent().length-1; j++){
					for(int i=0; i<putArmies.getAdjacent().length-1;i++){
						if(takeArmies.getAdjacent()[j].==putArmies.getAdjacent()[i] && takeArmies.getOccupyingPlayer().equals(putArmies.getOccupyingPlayer())){
							takeArmies.setPlayerArmies(takeArmies.getPlayerArmies()-amountMoved);
							putArmies.setPlayerArmies(putArmies.getPlayerArmies()+amountMoved);
						}
					}

				}
			}
		}

	}
}
*/
}


